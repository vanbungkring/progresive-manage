<div class="bg-light lter b-b wrapper-md">
    <h1 class="m-n font-thin h3"><%=title%></h1>
</div>
<div class="wrapper-md">
    <div class="col-md-8">
        <div class="panel panel-danger">
            <div class="panel-heading"><h4>Apakah Anda ingin menghapus kategori campaign?</h4></div>
            <div class="panel-body">
                <form role="form" method="POST" ui-jq="parsley">
                    <div class="row">
                        <div class="col-md-12">
                            <div class="form-group">
                                <h3><%= (typeof data.data!='undefined' && data.data.bank) ? data.data.bank.name : '' %></h3>
                                <h4><%= (typeof data.data!='undefined' && data.data.bank) ? data.data.bank.branchName : '' %></h4>
                                <h4><%= (typeof data.data!='undefined' && data.data.bank) ? data.data.bank.accNumber : '' %></h4>
                                <h4><%= (typeof data.data!='undefined' && data.data.bank) ? data.data.bank.holderName : '' %></h4>
                            </div>
                              <input type="text" class="hidden" value="<%= (typeof data.data!='undefined' && data.data._id) ? data.data._id : '' %>" name="_id">
                        </div>
                    </div>
                    <div class="line line-dashed b-b line-lg pull-in"></div>
                    <div class="form-group">
                        <div class="row">
                          <div class="col-xs-6">
                            <button type="submit" class="btn btn-danger">Ya</button>
                          </div>
                          <div class="col-xs-6">
                            <a href="<%=PREFIX_ROUTE_BACK_OFFICE%>/payment/account" class="btn btn-default pull-right">Tidak</a>
                          </div>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
